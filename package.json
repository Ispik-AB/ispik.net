{
  "name": "ispik.net",
  "version": "1.0.0",
  "description": "ispik.net website",
  "main": "index.js",
  "scripts": {
    "build:css": "tailwindcss -i ./src/input.css -o ./dist/output.css",
    "build:production": "tailwindcss --minify -i ./src/input.css -o ./dist/style.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Fredrik Thulin",
  "license": "BSD-2-Clause",
  "dependencies": {},
  "devDependencies": {
    "prettier": "^2.5.1",
    "tailwindcss": "^3.0.15"
  }
}
